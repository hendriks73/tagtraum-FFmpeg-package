<project default="ffmpeg">

    <target name="ffmpeg">

        <!-- unpack -->
        <untar src="${project.build.directory}/ffmpeg.tar" dest="${project.build.directory}/ffmpeg-${arch}-${host}"/>

        <!-- fix permissions messed up by untar -->
        <chmod perm="ugo+rx" dir="${project.build.directory}/ffmpeg-${arch}-${host}" includes="**/*.sh"/>

        <!-- configure -->
        <echo>./configure ${ffmpeg.configure} --target-os=${host} --arch=${arch} --cc=&quot;${cc}&quot; ${configure.options.additional}</echo>
        <exec dir="${project.build.directory}/ffmpeg-${arch}-${host}/ffmpeg-${project.version}"
              executable="${sh}"
              failonerror="true">
            <env key="PATH" path="${path}"/>
            <arg line="configure ${ffmpeg.configure} --target-os=${host} --arch=${arch} --cc=&quot;${cc}&quot; ${configure.options.additional}"/>
        </exec>
        <jar destfile="${project.build.directory}/ffmpeg-sources-${arch}-${host}.jar" basedir="${project.build.directory}/ffmpeg-${arch}-${host}/ffmpeg-${project.version}"/>

        <!-- make -->
        <exec dir="${project.build.directory}/ffmpeg-${arch}-${host}/ffmpeg-${project.version}"
              executable="${make}"
              failonerror="true">
            <env key="PATH" path="${path}"/>
        </exec>

        <!-- copy results -->
        <mkdir dir="${project.build.directory}/lib-${arch}-${host}"/>
        <copy todir="${project.build.directory}/lib-${arch}-${host}" flatten="true">
            <fileset dir="${project.build.directory}/ffmpeg-${arch}-${host}/ffmpeg-${project.version}">
                <include name="**/lib*.a"/>
                <include name="**/lib*.dylib"/>
                <include name="**/*.dll"/>
            </fileset>
        </copy>
    </target>

</project>

